dependencies:
  common-lib:
    version: 2.0.0
    usage:
      - parse_record(text, mode="lenient")
      - ParseResult.errors_and_value
    removed_assumptions:
      - legacy_mode
      - None_return_means_parse_failure
  common-schema:
    version: 1
