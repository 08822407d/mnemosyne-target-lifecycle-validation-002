# CommonLib 2.0 — human-facing changes

CommonLib 2.0 updates parser behavior and configuration. Parsing now returns a structured result, strict/lenient behavior is selected with `mode`, and the old `legacy_mode` key is removed. Projects using the parser should read `docs/CHANGES-AGENT.md` before upgrading.
